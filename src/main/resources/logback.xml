<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ── Console sink ──────────────────────────────── -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── File sink (rotates daily, keeps 30 days) ──── -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/discord-bot.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/discord-bot-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalCap>500MB</totalCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Noisy library silencers ───────────────────── -->
    <logger name="discord.jda" level="WARN"/>
    <logger name="okhttp3"     level="WARN"/>
    <logger name="com.zaxxer"  level="WARN"/>

    <!-- ── Root ──────────────────────────────────────── -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
